import SwiftUI

struct FineCheckView: View {
    var body: some View {
        NavigationStack {
            Text("Controlla la multa")
                .navigationTitle("Controlla la multa")
        }
    }
}
